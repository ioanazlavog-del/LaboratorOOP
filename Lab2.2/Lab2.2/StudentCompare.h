#pragma once
#include "Student.h"

int compareByName(const Student& a, const Student& b);
int compareByMath(const Student& a, const Student& b);
int compareByEnglish(const Student& a, const Student& b);
int compareByHistory(const Student& a, const Student& b);
int compareByAverage(const Student& a, const Student& b);